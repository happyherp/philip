# URL of the deployed Philip API (Cloudflare Pages) or local dev server
PHILIP_API_URL=https://your-project.pages.dev

# Optional: directory for session data and sessions.json (defaults to current dir)
DATA_DIR=.
